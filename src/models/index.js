const User = require('./user.model');
const Event = require('./event.model');
const Webhook = require('./webhook.model');
const FetchedRange = require('./fetchedRange.model');

module.exports = {
    User,
    Event,
    Webhook,
    FetchedRange,
};
